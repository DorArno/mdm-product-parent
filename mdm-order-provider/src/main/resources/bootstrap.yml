spring:
  application:
    name: order
  profiles:
    active: dev
skip.remote.servers: all
#  cloud:
#    config:
#      failFast: true
#      retry:
#        initialInterval: 3000
#        multiplier: 1.3
#        maxInterval: 5000
#        maxAttempts: 20
#      uri: ${vcap.services.${PREFIX:}configserver.credentials.uri:http://config.einwin.com:18888}